# 乡村应急广播与预警信息联动小程序 - 快速启动指南

## 一、技术架构
- **后端**：Spring Boot 3.2.0 + JPA + H2数据库 + JWT
- **前端**：Uni-app + Vue 3 + SCSS
- **开发工具**：HBuilderX、Trae、IntelliJ IDEA

## 二、环境要求

| 环境 | 版本 | 下载地址 |
|------|------|----------|
| JDK | 17+ | https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html |
| Maven | 3.6+ | https://maven.apache.org/download.cgi |
| HBuilderX | 最新版 | https://www.dcloud.io/hbuilderx.html |

### 环境验证
打开终端（CMD 或 PowerShell），执行以下命令确认环境已配置：

```powershell
java -version
# 输出示例：java version "17.0.1" 2021-10-19 LTS

mvn -version
# 输出示例：Apache Maven 3.8.1
```

## 三、快速启动

### 3.1 启动后端服务

#### 方法一：使用 Maven 直接启动（推荐）
```powershell
# 1. 进入项目后端目录
cd 第5组-《乡村应急广播与预警信息联动小程序设计》-13黄锡辉-18李智辉-08何梓恒-源代码\backend

# 2. 编译并启动后端服务
mvn spring-boot:run
```

#### 方法二：先编译打包再启动
```powershell
# 1. 进入后端目录
cd 第5组-《乡村应急广播与预警信息联动小程序设计》-13黄锡辉-18李智辉-08何梓恒-源代码\backend

# 2. 清理并重新编译
mvn clean compile

# 3. 启动服务
mvn spring-boot:run
```

#### 方法三：打包为 JAR 文件运行
```powershell
# 1. 进入后端目录
cd 第5组-《乡村应急广播与预警信息联动小程序设计》-13黄锡辉-18李智辉-08何梓恒-源代码\backend

# 2. 打包（跳过测试）
mvn clean package -DskipTests

# 3. 运行 JAR 包
java -jar target\village-alert-1.0.0.jar
```

**后端成功启动后，终端显示以下信息：**
```
Tomcat started on port 8080 (http) with context path ''
Started VillageAlertApplication in 4.0 seconds
```

- 服务地址：http://localhost:8080
- API 文档：http://localhost:8080/swagger-ui/index.html
- H2 控制台：http://localhost:8080/h2-console

### 3.2 停止后端服务

#### 方法一：在运行终端按 Ctrl+C
在启动后端的终端窗口中，按下 `Ctrl+C` 组合键，等待进程停止。

#### 方法二：通过端口号强制停止
```powershell
# 1. 查找占用 8080 端口的进程 PID
netstat -ano | findstr :8080
# 输出示例：TCP 0.0.0.0:8080 0.0.0.0:0 LISTENING 12345

# 2. 根据 PID 强制停止进程
taskkill /F /PID 12345
```

#### 方法三：停止所有 Java 进程
```powershell
taskkill /F /IM java.exe
```

### 3.3 重启后端服务
```powershell
# 第一步：停止旧进程
taskkill /F /IM java.exe

# 第二步：进入后端目录
cd 第5组-《乡村应急广播与预警信息联动小程序设计》-13黄锡辉-18李智辉-08何梓恒-源代码\backend

# 第三步：清理编译缓存并重新启动
mvn clean compile -q
mvn spring-boot:run
```

### 3.4 配置 HBuilderX

1. 安装插件（工具 → 插件安装）：
   - `scss/sass 编译` - 用于样式文件编译
   - `uni-app 小程序编译` - 用于小程序项目编译

2. 导入项目：
   - 打开 HBuilderX → 文件 → 导入 → 从本地目录导入
   - 选择项目源代码根目录

### 3.5 运行前端项目

| 运行方式 | 操作步骤 |
|----------|----------|
| **H5 浏览器** | HBuilderX → 顶部菜单 → 运行 → 运行到浏览器 → 选择 Chrome/Edge |
| **微信小程序** | HBuilderX → 顶部菜单 → 运行 → 运行到小程序模拟器 → 微信开发者工具 |

**注意**：运行前端前，请确保后端服务已成功启动（端口 8080）。

## 四、页面说明

### 系统管理员端
| 页面 | 路径 | 功能说明 |
|------|------|----------|
| 登录 | pages/admin/login/login | 管理员登录入口 |
| 首页 | pages/admin/index/index | 预警概况、任务统计、传感器总览 |
| 用户管理 | pages/admin/users/users | 用户列表、搜索、禁用/启用 |
| 预警管理 | pages/admin/alerts/alerts | 预警发布、编辑、删除、筛选 |
| 预警模板管理 | pages/admin/templates/templates | 预警模板创建、编辑、删除 |
| 数据统计 | pages/admin/statistics/statistics | 各类数据统计图表展示 |
| 传感器监控 | pages/admin/sensor/sensor | 传感器实时数据与预警记录 |
| 系统设置 | pages/admin/settings/settings | 公告发布、日志查看、数据备份 |

### 网格员端
| 页面 | 路径 | 功能说明 |
|------|------|----------|
| 登录 | pages/grid/login/login | 网格员登录入口 |
| 注册 | pages/grid/register/register | 网格员注册 |
| 首页 | pages/grid/index/index | 任务统计仪表盘、待处理任务概览 |
| 任务列表 | pages/grid/tasks/tasks | 任务列表与状态筛选 |
| 任务详情 | pages/grid/task-detail/task-detail | 任务确认与处置反馈 |
| 预警查看 | pages/grid/alerts/alerts | 预警信息列表查看 |
| 个人中心 | pages/grid/profile/profile | 用户信息与设置 |

### 村民端
| 页面 | 路径 | 功能说明 |
|------|------|----------|
| 登录 | pages/villager/login/login | 用户登录入口 |
| 注册 | pages/villager/register/register | 新用户注册 |
| 首页 | pages/villager/index/index | 预警概览展示 |
| 预警列表 | pages/villager/alerts/alerts | 预警信息列表查看 |
| 预警详情 | pages/villager/alert-detail/alert-detail | 预警详情与语音播报 |
| 安全指南 | pages/villager/guides/guides | 安全知识指南列表 |
| 指南详情 | pages/villager/guide-detail/guide-detail | 指南详细内容 |
| 紧急求助 | pages/villager/help/help | 一键求助与位置共享 |
| 个人中心 | pages/villager/profile/profile | 用户信息管理 |

## 五、预置账户

| 用户名 | 密码 | 角色 | 所属端 | 登录后跳转 |
|--------|------|------|--------|-----------|
| admin | password | 管理员 | 系统管理员端 | 系统管理员端首页 |
| grid001 | password | 网格员 | 网格员端 | 网格员端首页 |
| village001 | password | 村民 | 村民端 | 村民端首页 |

## 六、常见问题

### 6.1 端口占用
```powershell
# 查看端口占用
netstat -ano | findstr :8080

# 强制停止占用进程
taskkill /F /PID <PID>
```

### 6.2 后端编译失败
```powershell
# 清理缓存后重新编译
cd backend
mvn clean compile -q
mvn spring-boot:run
```

### 6.3 后端启动后自动退出
- 检查是否已有进程占用 8080 端口
- 检查 JDK 版本是否为 17+
- 检查 Maven 版本是否为 3.6+

### 6.4 前端 API 请求失败
1. 确认后端服务是否已启动（访问 http://localhost:8080 测试）
2. 确认前端项目中 `utils/api.js` 中的 baseURL 是否为 `http://localhost:8080`
3. 清除浏览器缓存或小程序缓存后重试

### 6.5 HBuilderX 运行报错
- 确保已安装 `scss/sass 编译` 插件
- 确保项目路径不包含特殊字符
- 如运行到小程序，确保已安装微信开发者工具
