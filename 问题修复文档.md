# 乡村应急广播与预警信息联动小程序 - 问题修复文档

## 一、后端问题

### 1.1 JDK版本不兼容
- **问题**：编译失败，JDK版本过低
- **解决**：安装JDK 17+，配置JAVA_HOME

### 1.2 BCryptPasswordEncoder找不到
- **问题**：密码加密类找不到
- **解决**：在pom.xml添加spring-security-crypto依赖

### 1.3 JWT密钥长度不足
- **问题**：Token生成失败
- **解决**：配置32字节以上的密钥

### 1.4 端口占用
- **问题**：8080端口被占用
- **解决**：`netstat -ano | findstr :8080` → `taskkill /F /PID <PID>`

### 1.5 数据初始化失败
- **问题**：data.sql执行时表不存在
- **解决**：配置`defer-datasource-initialization: true`

## 二、前端问题

### 2.1 密码输入显示问题
- **问题**：密码输入框显示明文
- **解决**：设置`type="password" password="true"`

### 2.2 中文乱码问题
- **问题**：页面中文显示乱码
- **解决**：确保文件编码为UTF-8

### 2.3 搜索功能异常
- **问题**：搜索时空值导致错误
- **解决**：添加null安全检查

### 2.4 微信小程序网络请求失败
- **问题**：域名校验不通过
- **解决**：开发环境勾选"不校验合法域名"

### 2.5 网格员端页面跳转失败
- **问题**：`switchTab:fail can not switch to no-tabBar page`
- **原因**：`pages.json` 的 `tabBar` 配置中只有村民端页面，网格员端页面不在 tabBar 列表中，`switchTab` 只能跳转到 tabBar 页面
- **解决**：将网格员端页面的跳转方式从 `uni.switchTab()` 改为 `uni.reLaunch()`
- **修改文件**：
  - `pages/grid/index/index.vue`
  - `pages/grid/profile/profile.vue`
  - `pages/grid/login/login.vue`

### 2.6 App.vue 方法调用失败
- **问题**：`app.setUserInfo is not a function` / `app.clearUserInfo is not a function`
- **原因**：在 uni-app 中，`getApp()` 返回的对象不直接包含方法，需要通过 `globalData` 访问
- **解决**：在 `App.vue` 的 `onLaunch` 中将方法挂载到 `globalData`，调用时通过 `app.globalData.setUserInfo()` 访问
- **修改文件**：
  - `App.vue`
  - `pages/villager/profile/profile.vue`
  - `pages/grid/profile/profile.vue`
  - `pages/villager/login/login.vue`
  - `pages/grid/login/login.vue`

### 2.7 注册账号数据丢失
- **问题**：注册过的账号下次登录数据丢失
- **原因**：H2数据库使用内存模式 (`jdbc:h2:mem:village_alert`)，服务器重启后数据清空
- **解决**：修改 `application.yml`，将数据库改为文件存储模式 (`jdbc:h2:file:./data/village_alert`)
- **修改文件**：`backend/src/main/resources/application.yml`

### 2.8 提交预警失败 500 错误
- **问题**：提交预警时返回 500 错误
- **原因**：后端需要 `X-User-Id` header，但前端请求未传递该参数
- **解决**：在 `utils/api.js` 的请求工具中添加 `X-User-Id` header，从 localStorage 中获取用户ID
- **修改文件**：`utils/api.js`

## 三、安全问题

### 3.1 密码明文存储
- **风险**：密码泄露
- **解决**：使用BCryptPasswordEncoder加密

### 3.2 JWT密钥硬编码
- **风险**：密钥泄露
- **解决**：生产环境使用环境变量存储

## 四、调试技巧

### 4.1 查看数据库
- H2控制台：http://localhost:8080/h2-console
- JDBC URL: `jdbc:h2:mem:village_alert`

### 4.2 API测试
```powershell
Invoke-RestMethod -Uri 'http://localhost:8080/api/auth/login' `
  -Method Post -ContentType 'application/json' `
  -Body '{"username":"admin","password":"password"}'
```